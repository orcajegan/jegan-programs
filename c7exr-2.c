#include <stdio.h>

int main (void)
{
   int i;
   int values[i];
   int index;
   int n = 10;

   for ( int i = 0; i <= n; ++i){
     values[i] = i;
     return 1;
      }

    for ( index = 0; index < 10; ++index )
    printf ("values[%i] = %i\n", index, values[index]); 
    return 0;
}
